# 1. Instale o Reanimated corretamente (usando o Expo)
npx expo install react-native-reanimated

# 2. Instale também o worklets (necessário na versão mais nova)
npx expo install react-native-worklets

# 3. Carousel mais moderno e estável (melhor que o snap-carousel antigo)
npx expo install react-native-reanimated-carousel

npx expo start -c


back 
npm init -y
npm install express pg bcryptjs jsonwebtoken cors dotenv
npm install -D typescript ts-node-dev @types/express @types/pg @types/bcryptjs @types/jsonwebtoken @types/cors

rodar npm run dev e ligar run no neon